
	<ul id="links">
		<% @links.each do |link| %>
			<%= partial :link, :locals => {:link => link} %>
			<!-- <li><a href="<%= link.url %>"><%= link.title %></a> -->
		<!-- </li> -->
		<% end %>
	</ul>

